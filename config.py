"""
Proxy Redirector — Configuration
إعدادات البرنامج المركزية
"""

# ──────────────────────────────────────────────
# إعدادات المجموعة (Pool)
# ──────────────────────────────────────────────
MIN_ALIVE_POOL = 3                     # أقل عدد بروكسيات يعملون (إذا نقص يبحث عن المزيد)
BATCH_SIZE = 50                        # عدد البروكسيات في كل دفعة فحص
RECHECK_INTERVAL_SECONDS = 60          # إعادة فحص البروكسيات العاملة كل 60 ثانية

# ──────────────────────────────────────────────
# إعدادات الفرصة الثانية (Retry / Rehabilitation)
# ──────────────────────────────────────────────
DEAD_RETRY_AFTER_SECONDS = 300         # إعادة فحص الميت بعد 5 دقائق
MAX_CONSECUTIVE_FAILURES = 5           # بعد 5 فشلات متتالية = مُستبعد مؤقتاً
BLACKLIST_AFTER_FAILURES = 15          # بعد 15 فشل = مُستبعد نهائياً (لا يُفحص)
DEAD_RECHECK_BATCH = 10               # عدد البروكسيات الميتة لإعادة فحصها كل دورة

# ──────────────────────────────────────────────
# إعدادات الفحص
# ──────────────────────────────────────────────
CHECK_TIMEOUT_SECONDS = 8              # مهلة الفحص لكل بروكسي
MAX_CONCURRENT_CHECKS = 50             # أقصى عدد فحوصات متزامنة في الدفعة
CHECK_URL = "http://httpbin.org/ip"    # URL المستخدم للفحص
ANONYMITY_CHECK = True                 # التحقق أن البروكسي يخفي IP الحقيقي

# ──────────────────────────────────────────────
# إعدادات السيرفر المحلي
# ──────────────────────────────────────────────
LOCAL_HOST = "0.0.0.0"                 # يسمع على كل الواجهات (للوصول من الشبكة)
LOCAL_PORT = 1080                      # بورت SOCKS5 المحلي
HTTP_PROXY_PORT = 8080                 # بورت HTTP Proxy (للهواتف)

# ──────────────────────────────────────────────
# حماية المصادقة (SOCKS5 Auth)
# ──────────────────────────────────────────────
AUTH_ENABLED = True                    # تفعيل المصادقة بكلمة مرور
AUTH_USERNAME = "admin"                # اسم المستخدم — غيّره!
AUTH_PASSWORD = "proxy2026"            # كلمة المرور — غيّرها!

# الشبكات المسموح لها بدون مصادقة (للهواتف التي لا تدعم إدخال كلمة مرور)
# أضف شبكتك المحلية هنا
AUTH_WHITELIST = [
    "192.168.100.",                    # شبكتك المحلية
    "192.168.1.",                      # شبكة الراوتر الداخلية
    "192.168.0.",                      # شبكة الراوتر الداخلية
    "127.0.0.1",                       # الجهاز نفسه
]

# ──────────────────────────────────────────────
# إعدادات Failover
# ──────────────────────────────────────────────
FAILOVER_MAX_RETRIES = 3               # عدد المحاولات قبل التبديل
FAILOVER_RETRY_DELAY = 1               # ثوان بين المحاولات

# ──────────────────────────────────────────────
# ملفات
# ──────────────────────────────────────────────
PROXIES_FILE = "data.json"
STATUS_FILE = "proxies_status.json"

# ──────────────────────────────────────────────
# إعدادات الترتيب (Score Weights)
# ──────────────────────────────────────────────
SCORE_ALIVE = 50                       # نقاط إذا كان يعمل
SCORE_SPEED_MAX = 25                   # أقصى نقاط للسرعة
SCORE_RECENCY_MAX = 15                 # أقصى نقاط لحداثة النجاح
SCORE_FAILURE_PENALTY = 5              # خصم لكل فشل متتالي
SCORE_SUCCESS_RATE_MAX = 10            # أقصى نقاط لنسبة النجاح

# حد السرعة: بروكسيات أقل من هذا الرقم (ms) تحصل على أقصى نقاط سرعة
SPEED_THRESHOLD_MS = 500

# ──────────────────────────────────────────────
# IP الحقيقي (يُكتشف تلقائياً عند التشغيل)
# ──────────────────────────────────────────────
REAL_IP: str | None = None             # يُملأ تلقائياً — لا تعدّل يدوياً
